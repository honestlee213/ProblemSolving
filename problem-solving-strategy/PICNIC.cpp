#include <iostream>
#include <vector>
using namespace std;

int friendship(int ) {

}

int main() {
    int c, n, m;

    cin >> c;

    for (int i = 0; i < c; i++) {
        cin >> n;
        cin >> m;

        vector<vector<int>> kid(n, vector<int>(1, 0));
        int kid_1, kid_2;
        for (int j = 0; j < m; j++) {
            cin >> kid_1;
            cin >> kid_2;

            kid[kid_1].
            kid[kid_2][kid_1] = 1;
        }

        for (int j = 0; j < n; j++) {
            
        }
    }

    return 0;
}
